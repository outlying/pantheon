/**
 * Base configuration for Android Application modules
 *
 * This was added to have separate configuration for apps variations:
 * phones, watches, TV, tablets
 */
plugins {
    id "com.android.application"
    id "base.android.convention"
}

android {

}

dependencies {
    // UI related dependencies, as app modules are about UI
    implementation(project(":common:ui"))
}